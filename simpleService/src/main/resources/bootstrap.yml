spring:
  application:
    name: simpleService
  cloud:
    config:
      uri: http://localhost:8888/
      profile: dev
      label: master

server:
  port: 9999

management:
  security:
    enabled: false