spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/stlwtr/springcloud-config.git
          default-label: master
          search-paths: config